# jmr-260503 Branch Build Summary

Date: 2026-05-03

## Branches Merged (in order)

1. **jayemar/unread-count-v2** - Merged cleanly
2. **jayemar/share-article-as-markdown-link** - Merged cleanly
3. **jayemar/update-articles-on-start-and-at-intervals** - Merged cleanly
4. **jayemar/add-tag-indicator** - Several conflicts resolved
5. **jayemar/in-article-text-search** - Several conflicts resolved
6. **jayemar/order-by-insertion-date** - SQL and ViewModel conflicts resolved
7. **jayemar/article-summary-length-setting** - NEW this build; settings panel conflicts
8. **jayemar/pull-up-to-refresh-articles** - Largest conflict set; 8 files

## Merge Conflicts and Resolutions

### jayemar/add-tag-indicator
- `ArticleListVerticalSwipe.kt`: HEAD added `MARK_ALL_READ` value; used HEAD
- Various display settings: kept HEAD's sub-screen navigation approach

### jayemar/in-article-text-search
- `ArticleView.kt`: Feature moved TopBar/BottomBar inside inner Box; applied HEAD's `isReadLater` rename to the already-moved components
- `ArticleActions.kt`: Kept HEAD's icon imports, added feature's `Search` icon and `LocalFindInPage`
- `CopyToClipboard.kt`: Combined HEAD's new clipboard API with feature's `CopyLinkFormat`

### jayemar/order-by-insertion-date
- SQL files: Used feature's CASE statement ORDER BY for dynamic sort field
- `ArticleScreenViewModel.kt`: Changed `refreshInterval == MANUALLY_ONLY` to `appPreferences.refreshOnStart.get()`

### jayemar/article-summary-length-setting
- `ArticleListSettings.kt`: Kept both `showUnreadCount` (HEAD) and `shortenSummaries`/`summaryMaxLines` (feature)
- `DisplaySettingsViewModel.kt`: Added both sets of properties and update functions
- `strings.xml`: Added summary max lines strings

### jayemar/pull-up-to-refresh-articles
- `ArticleListVerticalSwipe.kt`: Merged HEAD's `MARK_ALL_READ` with feature's `REFRESH_ARTICLES`
- `ArticleList.kt`: Kept HEAD's Box wrapper and dynamic listHeight; feature removed `onGloballyPositioned`
- `ArticleScreen.kt`: Kept HEAD's `ArticleListScaffold`+`SwipeUpActionBox` approach; added `REFRESH_ARTICLES` case to `onSwipeUp`
- `ArticleScreenViewModel.kt`: Kept HEAD's `withUIContext` import; kept HEAD's `sidebar` Flow in `sidebarListener` but used feature's `!= NEXT_FEED` condition
- `ArticleView.kt`: Kept HEAD's `indicatorPadding`; dropped `icon` parameter (SwipeRefresh no longer has it)
- `SwipeRefresh.kt`: Used feature's `Drag` scroll handling (instead of HEAD's `UserInput`)
- `strings.xml`: Kept both `mark_all_read` and `refresh_articles` strings

## Build Fixes After Merges

- `ArticleList.kt`: Added missing `onGloballyPositioned`/`LocalDensity` imports and `listHeight`/`localDensity` variable declarations (HEAD kept dynamic height tracking but declarations were in a conflict context)
- `ArticleScreen.kt`: Fixed `pinnedScrollBehavior()` call to `TopAppBarDefaults.pinnedScrollBehavior()`; reordered `refreshAll()` local function before `onSwipeUp()` (Kotlin requires forward declaration for local functions); removed spurious `)` from conflict resolution
- `ArticleListSettingsPanel.kt`: Added missing `shortenSummaries`/`summaryMaxLines` fields to `ArticleListOptions` constructor call

## Notes for Future Builds

- `jayemar/article-summary-length-setting` was new this build - required fixing `ArticleListSettingsPanel.kt` caller
- `jayemar/pull-up-to-refresh-articles` removed the `icon` parameter from `SwipeRefresh`; `ArticleView.kt` no longer passes it (keep `indicatorPadding`)
- `SwipeRefresh.kt` now uses `Drag` instead of `UserInput` for scroll source - this is intentional from the pull-up branch
- `sidebarListener` in ViewModel should use `!= NEXT_FEED` condition (not just `== DISABLED`) to handle all non-navigation swipe modes correctly
- Local functions in composables must be declared before their first call site
